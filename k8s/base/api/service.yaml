apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: devops-app
  labels:
    app.kubernetes.io/name: api
spec:
  selector:
    app.kubernetes.io/name: api
  ports:
    - name: http
      port: 8000
      targetPort: http
